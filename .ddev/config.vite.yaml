# Exposes the Vite dev server (container port 5173) through ddev-router so
# HMR works at https://band.ddev.site:5173. Run the dev server with:
#   ddev npm run dev
web_extra_exposed_ports:
  - name: vite
    container_port: 5173
    http_port: 5172
    https_port: 5173
